<view class="tab-bar">
  <view
    wx:for="{{ list }}"
    wx:key="index"
    class="tab-item {{active === index ? 'active' : ''}}"
    bindtap="onTabChange"
    data-index="{{ index }}"
  >
    <view class="tab-text">{{ item.text }}</view>
  </view>
</view>
